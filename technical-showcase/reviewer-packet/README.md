# Figurestead V2 Technical Showcase — review closure

This packet describes the corrected local-only V2 technical proof. V1 remains the concise reference; V2 remains undeployed and is not public navigation or an Overview replacement.

## Decision record

- Section 02’s key is generated from the line contract’s `resolveSeriesStyles()` output. The browser gate compares each rendered key entry with the resolved color, glyph, and line style. The fifth identity remains `ring + dash`; no marker vocabulary or public API changed.
- The default hidden accessibility companion retains noninteractive data tables, has no disclosure control in its clipped region, and omits a redundant single-panel heading. The deliberate visible mode still provides a focusable `View data` disclosure.
- The CVD plate uses the terms *Machado severity-1 protanomaly simulation*, *deuteranomaly simulation*, and *tritanomaly approximation*. It is simulation-only, not medical/accessibility certification.
- Intermediate motion captures are a recorded restrained-motion sequence from a free-running clock. Requested and observed progress are recorded; intermediate hashes are not machine-independent deterministic proof. Normalized and rendered terminal invariance remain enforced.
- Panel D uses stable site codes only on that dense axis. The shared key carries the full names.
- Paper composition remains `grid-template-columns: 89fr 183fr`. R3’s accepted lossless derivatives are served, canonical PNGs remain directly linked, and the measured 6.372 pt label is compared with the Figurestead 6 pt project floor. This remains digital evidence, not physical printer certification.

## Verification result

The browser matrix contains eight zero-finding cases: Chromium and Firefox at 390 and 1440 CSS px, each with normal and reduced motion. It checks runtime/console errors, horizontal overflow, narrow source order, report-key source-of-truth, companion headings/tables, ancestor clipping and positioned-offscreen focus, full keyboard traversal, reduced-motion immediacy, normalized and rendered terminal invariance, CVD metadata, paper ratio and routes, Panel D codes, semantics, and scope boundaries.

Reference 1440 Chromium terminal fingerprints:

- Normalized Static / Restrained / Expressive: `fnv1a32-c46d7647`
- Rendered Static / Restrained / Expressive: `fnv1a32-90471c0c@992x538`

Resolved report key:

| Site | Color | Glyph | Line |
| --- | --- | --- | --- |
| Headwater | `#1B4C8A` | ring | solid |
| Forest tributary | `#143F33` | square | solid |
| Agricultural fork | `#0E766E` | triangle | solid |
| Urban reach | `#8B7A16` | diamond | solid |
| Estuary | `#3C97AC` | ring | dash |

Paper delivery totals fell from 15,406,891 canonical bytes to 384,171 served bytes (97.506%). Each full-size optimized PNG decodes to exactly the canonical RGBA pixels.

The CVD assumption check used Node `v24.14.0`, Python `3.13.13`, and `colour-science 0.4.7`. Maximum absolute matrix/sample-vector delta was on the order of `10^-17`, passing the declared `1e-12` threshold. This is a narrow matrix-and-vector agreement gate, not independent perceptual validation.

V1’s current legacy aggregate exactly matches the accepted anchor `2f0403348e9b2e18e120d47b5fedb3f24062a978e0a02b2af8e7c62a28a553a7`. New evidence also records a 60-file OS-artifact-excluded aggregate; no OS artifact is listed in this packet. Public R3 matches baseline commit `a2cb55c7813c9ba7040aba58099e6dc626ae5846`, with site tree `c65d33a1a72ce18b8a756ac007f9117e6336f28d` and no changed public path.

## Evidence index

- `../evidence/summary.json` — two-engine matrix summary
- `../evidence/chromium.json`, `../evidence/firefox.json` — case-level structure, accessibility, motion, and runtime evidence
- `../evidence/motion-frames/sequence.json` — recorded restrained-motion thresholds, observed progress, and hashes
- `../evidence/cvd-reference-validation.json` — versions, tolerance, matrices, and sample-vector agreement
- `../evidence/preservation.json` — paper hashes/pixels, V1 anchor, R3 immutability, and packet hygiene
- `findings-before-after.json` — reproduced findings and closure status

## Held product decision

A possible fifth default glyph remains a separately authorized product-semantic decision. This pass did not change `MARKER_TYPES`, `GLYPH_CYCLE`, Canvas/SVG semantics, or the public marker API.
