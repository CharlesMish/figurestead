# Accepted readability outward visual refresh

This is the retained historical packet accepted at
`e14d9098f0daefebadf3c0637dbe14e6b5c937e9`. The later production
responsive-header refresh is recorded separately in
`docs/responsive-header-production-review/` so this packet's before/after
artifacts are not rewritten to conceal a second visual change.

This packet compares the outward README montage and owner-selected social
preview at the accepted pre-refresh commit
`a8f7831aad1c77c5b4577daf0cfb0c7752347833` with assets rendered after the
compact 14 px title floor and screen provenance legibility treatment. The full-size pairs preserve source pixels; the
README-width and 25% pairs apply only deterministic Lanczos downscaling.

The refresh changes no composition, crop rule, corpus data, theme token,
package version, or public-site surface. `manifest.json` records every
affected compact specimen frame and exact old/new hash. The populated matrix
is verified byte-identical and is not regenerated.

Candidate C remains the owner-selected GitHub social-preview composition.
GitHub repository settings remain a manual owner action.

## Visual assessment

The current montage remains compositionally identical to the previous accepted
image at full resolution and at the representative 768 px-per-image comparison
width. Differences are limited to the documented title/provenance readability
treatment; scientific geometry and data are unchanged.

At 25% delivery size, both previous and current C retain an immediately legible
`Figurestead` wordmark. The current lead watershed figure remains meaningfully
inspectable, the circadian and seasonal supporting figures still communicate
range, and no title, annotation, or panel edge is newly clipped.
