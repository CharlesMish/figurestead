# GitHub README visual-story review packet

This packet compares the repository README at accepted baseline
`0f077b7fd5ab4840e73b65c133f09bad89187e39` with the documentation-only
GitHub visual-story pass.

## Before

- Eight lines of package/status prose.
- No installation command or runnable example.
- No visual evidence.

## After

- Identity and alpha status appear before the first section.
- `## Start here` is the first section; the pinned Python install command begins
  at README line 12 and the browser package command at line 27.
- The accepted eight-panel montage is the first major visual.
- The populated categorical response matrix follows at full readable width.
- Runtime asymmetry, project-authored limits, synthetic provenance, and deeper
  evidence routes remain discoverable without delaying first success.

The HTML files are deterministic GitHub-like local renderings used only for
visual review. PNG screenshots and the machine-readable audit/manifests are
generated evidence, not deployed pages.

The in-app browser's full-page capture stitched incorrectly while a temporary
viewport override was active. The packet therefore retains verified viewport
captures at 1440 and 390 CSS px, plus the full-resolution montage and matrix
assets themselves; it does not present the broken stitched capture as evidence.
